# Контент: что нужно от заказчика

Два блока: тексты на вычитку и список кадров для фотосъёмки.

## 1. Тексты

В вайрфрейме на месте абзацев стояли серые полосы — то есть текстов не было
вообще. Всё, что сейчас на странице, — **рабочий черновик**, написанный, чтобы
вёрстка жила на реальной длине строк. Его нужно вычитать и заменить.

Намеренно **не** написано: сроки работы на рынке, объёмы производства,
сертификаты, ГОСТы, названия поставщиков, гарантии доставки. Такие утверждения
можно добавить только с подтверждением от заказчика.

Файл: `src/data/content.ts`

| Секция | Что вычитать |
| --- | --- |
| Hero | Абзац под подзаголовком (`src/components/Hero.astro`) |
| О бренде | `about.body` — 3–4 строки о производстве |
| О бренде, 3 пункта | `features[].text` — по строке на пункт |
| Продукция | `products[].description` — по 2 строки на вид рыбы |
| Почему нас | `advantages[].text` — по 2 строки |
| Оптовым клиентам | `wholesale.body` — 3 строки |
| CTA-полоса | `ctaBand.body` |
| SEO | `site.description` в `src/data/site.ts` |

Формы заявки на сайте нет — сбор персональных данных через веб-форму требует
регистрации оператора персональных данных (152-ФЗ), решили не делать. Вся
коммуникация — через контакты в разделе «Контакты».

Заголовки, подписи и надписи на кнопках зафиксированы макетом — их менять не
нужно, они перенесены дословно.

## 2. Фотографии

Класть в `public/images/`, затем прописать `src` в `src/data/content.ts`
(см. README).

### Hero — 1 кадр
- Крупный план рыбы, тёмное дерево, дым. Кадр тёмный, горизонтальный,
  full-bleed. Слева поверх ляжет тёмная маска с текстом — левая треть кадра
  должна быть спокойной, без важных деталей.
- Ориентир: 2560×1440, приоритетная загрузка.

### О бренде — 1 кадр
- Процесс копчения. Горизонталь, пропорции ≈ 2:1.

### Ассортимент — 6 кадров, пропорции 4:3 (готово)
- форель · кета · горбуша · скумбрия · сельдь · ассорти (для «Другие виды»)
- Единый свет и фон: карточки стоят рядом в сетке, разнобой будет заметен.

### Галерея — 4 кадра
- готовая рыба крупным планом (вертикальный, занимает две строки)
- процесс копчения (широкий, на две колонки)
- упаковка (небольшой)
- отгрузка (небольшой)

### CTA-полоса — 1 кадр
- Крупное фото готовой рыбы. Как и в hero, слева ляжет тёмная маска с текстом.

### og:image — 1 кадр
- 1200×630 для превью в мессенджерах и соцсетях.

**Итого 7 кадров на странице (6 готово) + 1 для og:image.**

Все `alt`-тексты уже написаны и лежат рядом с описанием кадра в
`src/data/content.ts`.

## 3. Логотип

- SVG в двух вариантах: на светлом фоне и на тёмном.
- Сейчас на месте логотипа заглушка: квадрат-контур с волновым мотивом.

## 4. Контакты

`src/data/site.ts` → `contacts`. Телефон, адрес и режим работы уже заполнены.
Не хватает прямых ссылок на Telegram / WhatsApp / MAX (сейчас у мессенджеров
указан только номер текстом, без `href`) — их можно добавить, когда появятся
конкретные аккаунты/чаты.
